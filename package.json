{
  "name": "complete-infiverse",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "install:client": "npm --prefix client install --legacy-peer-deps",
    "build:client": "npm --prefix client run build",
    "build": "npm run install:client && npm run build:client"
  },
  "dependencies": {
    "date-fns": "^3.6.0",
    "react-day-picker": "^9.4.4"
  }
}
